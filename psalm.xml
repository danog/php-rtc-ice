<?xml version="1.0"?>
<psalm
    errorLevel="1"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor-bin/psalm/vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
>
    <projectFiles>
        <directory name="src"/>
        <ignoreFiles>
            <directory name="vendor"/>
            <directory name="vendor-bin"/>
        </ignoreFiles>
    </projectFiles>
    <issueHandlers>
        <!-- Typed class constants require PHP 8.3. This package still supports 8.2, and Psalm
             only reports MissingClassConstType for non-final classes, so RTCIceConnection's
             constants cannot be given native types without dropping 8.2. -->
        <MissingClassConstType errorLevel="suppress"/>
    </issueHandlers>
</psalm>
